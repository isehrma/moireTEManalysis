# moireTEManalysis

The filtration DM script is intended to be used on a STEM image. Then, the resulting images can be converted through "batch convert" option. The exported images can be used for moire map creation using Python.